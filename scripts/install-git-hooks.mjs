import { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, eSimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSync, mkdirSynimport { chmodSync, copyFileSynckens, themes, layout, components, utilities, and print stylesheet.
- Dark, light, and system theme modes using `data-theme` on the document root.
- Module boundaries for `ui`, `services`, `repositories`, `domain`, `db`, `workers`, `crypto`, `sync`, `reports`, `validation`, and `migrations`.
- Sensitive-file and sample-data scans for repository protection.

## Development

```sh
npm install
npm run dev
npm run build
npm run test
```

## Security Rule

Do not commit real customer data, addresses, bittings, pinning records, keyholder data, SQLite databases, spreadsheets, CSV exports, or `.mkdb` backups. Treat plaintext exports as dangerous.

Theme preference may be stored in `localStorage`; master-key data must not be stored there.
